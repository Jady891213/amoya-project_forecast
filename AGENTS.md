# 项目协作说明

## 项目目标

本项目用于继续设计和实现“项目测算分析工具”。开始开发前先阅读：

- `README.md`
- `docs/design/项目测算分析工具_需求与历史产物梳理.md`
- `docs/design/项目测算分析工具_产品结构与交互方案.md`
- `docs/design/项目测算分析工具_业务建模与多维执行方案.md`
- `docs/design/项目测算分析工具_项目中心与预测工具台方案.md`
- `docs/design/项目测算分析工具_开发阶段与任务清单.md`
- `docs/design/项目测算分析工具_P0数据契约.md`
- `docs/design/项目测算分析工具_P1A预测配置与计算契约.md`
- `docs/design/DeepSeek对话_盈亏平衡与多场景需求提取.md`

## 当前开发基线

- 根目录 React + TypeScript + Vite 工程是当前可运行开发基线。
- `docs/design/项目测算分析工具_开发阶段与任务清单.md` 是当前开发范围、优先级和验收状态的持续维护基线；阶段完成或范围调整后同步更新。
- `docs/design/项目测算分析工具_P0数据契约.md` 是主数据、事实、默认指标、报表查询和参考数据隔离的当前契约。
- `docs/design/项目测算分析工具_P1A预测配置与计算契约.md` 是预测行、计算批次、结果替换和计算页面的当前契约。
- `src/mocks/p0ReferenceDataset.ts` 是P0历史项目主数据和结构验证事实的唯一初始化来源；不要把初始化数值散落到业务组件。
- `src/services/forecastCompiler.ts` 是两种预测方式展开为分月行项目结果的确定性编译入口；不得在页面中复制计算口径。
- `docs/prototype/项目测算分析工具_项目中心与预测工具台原型.html` 是当前页面结构与布局基线；顶栏、左侧导航、项目中心和进入项目后的工作区层级应优先与其保持一致。
- `docs/prototype/index.html` 只参考局部UI风格和历史业务内容，不得用它替换当前布局基线。
- `docs/prototype/项目测算分析工具_四套交互方案.html` 和 `docs/prototype/项目测算分析工具_业务建模与多维执行原型.html` 是上一阶段探索，仅作为演进参考。
- `references/historical_deliverables/` 是需求证据和对外产物参考，不要直接覆盖或改写原文件。

## 开发与验证

```bash
pnpm dev
pnpm test
pnpm build
pnpm build:singlefile
```

- 普通构建输出到 `dist/`。
- 单 HTML 输出到 `dist-singlefile/index.html`，使用内存 SQLite，只作为便携体验与数据库文件交换入口，不作为自动持久化验收环境。
- 自动化测试使用Vitest、SQLite WASM内存库、纯预测编译器和纯指标引擎测试。

## 业务原则

- 区分含税、不含税和现金流口径。
- 所有计算结果必须能追溯到明确假设、输入和公式。
- 产品采用“业务建模层 + 模型编译层 + 多维执行层 + 分析输出层”：财务使用行项目建模，系统内部编译为结构化多维结果。
- 外层提供“项目列表、归档项目、主数据管理、指标管理”；进入项目后显示独立项目工作区。
- 项目工作区采用“预测配置—计算表格—项目报表”三步结构；P1A 开放收入、成本行项目的固定月金额和逐月填写。
- 项目内固定为“预测配置—计算表格—图表报告”三个页面，不设置独立场景分析工作台。
- 方案比较作为报告章节，并与计算表格、报告、导出共同读取同一次编译结果，禁止维护平行的页面级计算口径。
- 预测配置以行项目配置表为主，主要配置直接回显，详细配置通过右侧挤压式表单编辑，不遮罩主表；计算表格单独只读展示行项目组成和分月损益。
- 预测配置通过“保存并计算”生成计算批次，成功后进入计算表格；计算表格不直接编辑模型，配置变化后必须重新计算。
- 冻结的多维结构包括项目、部门、业务模块、期间、场景、版本和指标；场景与版本是平台级维度，不得按项目复制；后台字段统一使用 `period`，前端显示“期间”。
- 项目首次保存后自动登记为只读项目主数据，主数据页面不允许另行创建或修改项目，避免双入口。
- 项目生命周期只区分测算中与已归档，版本是计算快照，不引入审核或审批流程。
- 当前不建设跨项目分析；单项目报表仍必须读取统一多维事实结果，后续出现明确需求时再增加跨项目汇总。
- P0 已完成并冻结“历史项目主数据—SQLite WASM/OPFS—隔离结构验证事实—系统指标计算—计算表格—单项目报表”数据契约。
- SQLite表前缀统一使用 `dim_`、`fact_`、`sys_`，预留 `cfg_` 和 `snap_`；`dim_project` 是项目唯一主表，不建立填报项目与维度项目两套副本。
- PWA是自动持久化正式运行方式；单HTML使用内存SQLite，必须通过 `.sqlite3` 导入导出保存修改。
- 项目必须引用可维护部门；新建项目默认没有事实数据，禁止自动附加结构验证结果。
- 历史项目主数据统一使用 `datasetId: p0-reference-v2`；结构验证事实只在初始化模块中维护，不在主数据界面展示“演示/来源”标签。
- 基础事实只存收入、成本、现金流入和现金流出；派生指标在查询时计算，不回写事实表。
- P1A 的 `cfg_forecast_line` 与 `cfg_forecast_value` 保存配置，`fact_forecast_line_value` 保存带行项目快照的分月结果，`fact_metric_value` 保存收入、成本汇总；页面不得跨层直接拼装结果。
- 保存草稿必须保留上一批成功结果并标记过期；保存并计算成功后才替换当前项目的全局基准场景、工作版事实，失败必须保留旧结果。
- 首批预测方式只有 `monthly_input` 和 `fixed_monthly`；现金流、税率、行项目引用和 Excel 属于后续阶段，不得提前混入 P1A。
- 不要求财务用户理解或直接操作 Cube，界面继续使用行项目、分月表格、场景和版本等业务语言。
- 通用底座与专业模型模块分开，避免把所有历史字段堆进单一表单。
- 将“固定成本 + 单位变动成本 + 单价 × 用户数”的盈亏平衡模型作为分析模块，不要用它替代完整项目模型。
- 第一版以分月损益和分月现金流为核心，默认 12 个月并允许自定义到 36 个月。
- 场景采用“基准方案 + 参数覆盖”，统一 Excel 模板是平台标准交付物。
- 工具本身不提供 AI 功能；未来 Agent 可以调用确定性计算能力并在工具外总结。
- 当前 `index.html` 中的“AI 分析”是前端规则建议，不调用模型；正式产品应改用“测算建议”或同等准确名称。
- DeepSeek 对话中的示例参数、配色和 ECharts CDN 仅是原型参考，不是已确认的业务口径或技术选型。
- 历史 Excel 中的结果不能默认视为无误，尤其要检查跨表引用和 `#REF!`。
- “AI 分析”如仍是规则引擎，应明确标识，不要包装成真实模型调用。

## 工作区要求

- 临时产物名称包含 `TMP to delete`，用完主动清理。
- 交付物使用能够说明主题的文件名。
- 如建立 Git 仓库，提交说明主要使用中文。
- 浏览器自动化结束后关闭 browser/context/server，并检查无 Playwright 或 Chrome for Testing 残留进程。
