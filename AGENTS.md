# 项目协作说明

## 项目目标

本项目用于继续设计和实现“项目测算分析工具”。开始开发前先阅读：

- `README.md`
- `docs/00_文档导航.md`
- `docs/01_产品与设计/01_需求来源与业务目标.md`
- `docs/01_产品与设计/02_产品结构与用户流程.md`
- `docs/01_产品与设计/03_业务建模与多维数据模型.md`
- `docs/01_产品与设计/04_P0_SQLite数据底座契约.md`
- `docs/01_产品与设计/05_P1A_基础预测与计算契约.md`
- `docs/01_产品与设计/06_P1B_参数与公式契约.md`
- `docs/01_产品与设计/07_P1C_税与收付款规则契约.md`
- `docs/01_产品与设计/08_项目核心与正式输出契约.md`
- `docs/04_验收与进度/01_开发路线与阶段状态.md`
- `docs/04_验收与进度/02_P1A_历史项目回放与结果核对报告.md`
- `docs/04_验收与进度/03_P1B_参数与公式验收报告.md`
- `docs/04_验收与进度/04_P1C_税与现金流验收报告.md`
- `docs/04_验收与进度/05_本地服务与DB文件存储改造验收报告.md`
- `docs/04_验收与进度/06_项目核心与正式输出验收报告.md`
- `docs/04_验收与进度/07_六份历史素材回放与能力覆盖核对报告.md`
- `docs/05_后续计划/01_本地服务与DB文件存储改造.md`

## 当前开发基线

- `src/` 中的 React + TypeScript + Vite 工程是当前可运行开发基线；根目录只保留交付分区和用户启动入口。
- `docs/00_文档导航.md` 是文档唯一阅读入口；需求、契约、报告和原型不得重新混放。
- `docs/04_验收与进度/01_开发路线与阶段状态.md` 是当前开发范围、优先级和验收状态的持续维护基线；阶段完成或范围调整后同步更新。
- `docs/01_产品与设计/04_P0_SQLite数据底座契约.md` 是主数据、事实、默认指标、报表查询和参考数据隔离的当前契约。
- `docs/01_产品与设计/05_P1A_基础预测与计算契约.md` 是预测行、计算批次、结果替换和计算页面的当前契约。
- `docs/01_产品与设计/06_P1B_参数与公式契约.md` 是项目参数、公式语法、依赖计算和配置快照的当前契约。
- `docs/01_产品与设计/07_P1C_税与收付款规则契约.md` 是税口径、收付款规则、现金尾期和现金计划追溯的当前契约。
- `docs/01_产品与设计/08_项目核心与正式输出契约.md` 是语义接口、项目聚合保存、期间覆盖、财务表格和统一输出的当前契约。
- `docs/05_后续计划/01_本地服务与DB文件存储改造.md` 记录“本地服务 + 可见 `.db` 文件”的实施状态；macOS 已完成，目标 Windows 真机验证仍待执行。
- `src/server/index.ts` 是正式本地服务入口；`src/server/fileBackedSqliteClient.ts` 负责串行访问数据库并在每次写操作后同步 `data/amoya_project_forecast.db`。
- `src/app/api/client.ts` 是正式页面访问本地服务的语义 API Client；正式页面不得导入数据库表结构、`DatabaseClient` 或发送 SQL。
- `src/server/semanticApiRouter.ts` 和 `src/server/projectWorkspaceService.ts` 是正式 HTTP 与项目聚合应用服务入口；原始 SQL API 不得恢复。
- `src/shared/` 保存前后端共享 API 契约、领域类型和确定性计算引擎。
- `src/app/mocks/p0ReferenceDataset.ts` 只维护历史项目、部门和业务模块。
- `src/app/mocks/historicalProjectConfigs.ts` 是历史项目预测配置和源表逐月数据的唯一初始化来源；不得把历史数值散落到页面或仓储。
- `src/shared/calculation/forecastCompiler.ts` 是三种预测方式和期间覆盖展开为分月行项目结果的确定性编译入口；不得在页面中复制计算口径。
- `src/shared/calculation/formulaEngine.ts` 和 `formulaDependencyGraph.ts` 是公式解析、计算和依赖排序的唯一入口。
- `src/shared/calculation/cashScheduleCompiler.ts` 是将损益结算额编译为分月现金计划的唯一入口；页面不得自行拼装现金结果。
- `src/app/components/FinancialGrid.tsx` 是逐月输入、计算工作表和只读报告的统一财务表格底座。
- `src/server/services/reportWorkbookService.ts` 只根据 `ProjectReportDto` 生成统一 Excel，不得另建一套报表计算。
- `docs/03_原型/01_当前原型_项目中心与预测工作台.html` 是当前页面结构与布局基线；顶栏、左侧导航、项目中心和进入项目后的工作区层级应优先与其保持一致。
- `docs/02_需求素材/02_讨论与需求提取/` 和 `docs/03_原型/99_历史原型/` 是历史探索，仅作为演进参考，不得作为当前功能契约。
- `docs/02_需求素材/01_历史交付文件/` 是需求证据和对外产物参考，不要直接覆盖或改写原文件。

## 开发与验证

```bash
cd src
pnpm start:local
pnpm test
pnpm build
pnpm build:legacy-singlefile
```

- `pnpm start:local` 先构建前端，再启动 `127.0.0.1:4173` 本地服务并自动打开浏览器。
- 正式数据保存在 `data/amoya_project_forecast.db`；`data/*.db` 不提交 Git，升级源码不得覆盖用户数据库。
- 默认前端构建输出到 `output/web/`，只保留浏览器运行所需的 HTML、JavaScript 和 CSS。
- 单 HTML 输出到 `output/legacy-singlefile/index.html`，使用内存 SQLite，只作为旧兼容入口，不作为自动持久化验收环境。
- 自动化测试使用Vitest、SQLite WASM内存库、纯预测编译器和纯指标引擎测试。

## 业务原则

- 区分含税、不含税和现金流口径。
- 所有计算结果必须能追溯到明确假设、输入和公式。
- 产品采用“业务建模层 + 模型编译层 + 多维执行层 + 分析输出层”：财务使用行项目建模，系统内部编译为结构化多维结果。
- 外层提供“项目列表、归档项目、主数据管理、指标管理”；进入项目后显示独立项目工作区。
- 项目工作区采用“项目配置—计算工作表—项目报告”三步结构；项目配置页上方固定展示项目信息表单，下方集中维护损益预测、直接现金和项目参数。
- 项目内固定为“预测配置—计算表格—图表报告”三个页面，不设置独立场景分析工作台。
- 方案比较作为报告章节，并与计算表格、报告、导出共同读取同一次编译结果，禁止维护平行的页面级计算口径。
- 项目信息不再与预测类型并列为整页侧栏入口；其下方使用紧凑左侧切换“损益预测／直接现金／项目参数”，右侧展示明细表格。预测配置以行项目表为主，主要配置直接回显，详细配置通过表格右侧挤压式表单编辑，不遮罩主表；计算工作表允许对叶子期间值做可追溯覆盖，汇总和派生指标只读。
- 顶部“保存”一次保存整个项目草稿，“计算”在需要时先保存再生成批次；不得恢复项目信息和预测配置各自保存的分散操作。
- 计算工作表只允许对叶子行写入 `cfg_forecast_override`，不得直接覆盖原公式或派生指标；覆盖后必须重新计算。
- 冻结的多维结构包括项目、部门、业务模块、期间、场景、版本和指标；场景与版本是平台级维度，不得按项目复制；后台字段统一使用 `period`，前端显示“期间”。
- 项目首次保存后自动登记为只读项目主数据，主数据页面不允许另行创建或修改项目，避免双入口。
- 项目生命周期只区分测算中与已归档，版本是计算快照，不引入审核或审批流程。
- 当前不建设跨项目分析；单项目报表仍必须读取统一多维事实结果，后续出现明确需求时再增加跨项目汇总。
- P0 已完成并冻结“历史项目主数据—SQLite 基础事实—系统指标计算—计算表格—单项目报表”数据契约；持久化运行方式后续已切换为本地服务和可见 `.db` 文件。
- SQLite表前缀统一使用 `dim_`、`fact_`、`sys_`，预留 `cfg_` 和 `snap_`；`dim_project` 是项目唯一主表，不建立填报项目与维度项目两套副本。
- 本地 TypeScript 服务是自动持久化正式运行方式；页面查询和保存通过同源接口，并实时同步到 `data/amoya_project_forecast.db`。
- 旧单 HTML 使用内存 SQLite，必须手动导出才能保存修改，不能与正式服务的持久化承诺混用。
- 项目必须引用可维护部门；新建项目默认没有事实数据，禁止自动附加结构验证结果。
- 历史项目主数据统一使用 `datasetId: historical-project-config-v4`；事实必须由历史配置计算生成。当前六份历史文件中五份形成参考项目，CDN/广告敏感性模型不伪装成具体项目。
- 基础事实只存收入、成本、现金流入和现金流出；派生指标在查询时计算，不回写事实表。
- P1A 的 `cfg_forecast_line` 与 `cfg_forecast_value` 保存配置，`fact_forecast_line_value` 保存带行项目快照的分月结果，`fact_metric_value` 保存四类基础指标汇总；页面不得跨层直接拼装结果。
- 保存项目草稿必须保留上一批成功结果并标记过期；计算成功后才替换当前项目的全局基准场景、工作版事实，失败必须保留旧结果。
- 当前预测方式包括 `monthly_input`、`fixed_monthly` 和 `formula`；直接收款、付款计划复用同一套方式。
- 项目参数保存在 `cfg_parameter` 和 `cfg_parameter_value`，不进入全局指标维度；比例界面按百分数录入、数据库按小数保存。
- 公式只允许四则运算、括号、百分数字面量、参数引用和行项目引用；禁止任意 JavaScript、SQL 或项目专用函数。
- 公式计算必须检查缺失引用、逐月参数缺值、除零和循环依赖；计算批次保存不可变 `config_snapshot_json`。
- 收入成本统一以未税金额进入损益事实；含税行先除以 `1 + 税率`，未税行按税率还原含税结算额，免税行两者相同。
- 公式引用其他行项目时必须读取上游标准化后的未税金额。
- 收付款规则只支持不生成、当月、延后 0～36 个月和最多 12 期自定义分期；分期比例必须合计 100%，最后一期吸收尾差。
- `fact_cash_schedule_value` 保存规则现金的来源损益行、业务期间、税额、结算期和实收实付；直接现金和规则现金可以共存，但必须分别追溯。
- 损益行限制在经营期内，自动及直接现金允许延伸到经营期后 36 个月；报表必须标记回收期。
- 完整增值税核算、日期级账期、多场景、版本快照、Excel 输入和客户定制模板属于后续阶段；统一 Excel 输出已经完成。
- 历史项目必须先保存配置、再通过 `CalculationService` 生成事实；禁止恢复直接写入“结构验证事实”的做法。
- 历史项目核对以 `docs/04_验收与进度/07_六份历史素材回放与能力覆盖核对报告.md` 为当前总基线，P1A、P1B、P1C 报告保留阶段记录；源表冲突必须记录，不得用隐藏调整项强行对齐。
- 不要求财务用户理解或直接操作 Cube，界面继续使用行项目、分月表格、场景和版本等业务语言。
- 通用底座与专业模型模块分开，避免把所有历史字段堆进单一表单。
- 将“固定成本 + 单位变动成本 + 单价 × 用户数”的盈亏平衡模型作为分析模块，不要用它替代完整项目模型。
- 第一版以分月损益和分月现金流为核心，默认 12 个月并允许自定义到 36 个月。
- 场景采用“基准方案 + 参数覆盖”，统一 Excel 模板是平台标准交付物。
- 工具本身不提供 AI 功能；未来 Agent 可以调用确定性计算能力并在工具外总结。
- 当前 `index.html` 中的“AI 分析”是前端规则建议，不调用模型；正式产品应改用“测算建议”或同等准确名称。
- DeepSeek 对话中的示例参数、配色和 ECharts CDN 仅是原型参考，不是已确认的业务口径或技术选型。
- 历史 Excel 中的结果不能默认视为无误，尤其要检查跨表引用和 `#REF!`。
- “AI 分析”如仍是规则引擎，应明确标识，不要包装成真实模型调用。

## 工作区要求

- 临时产物名称包含 `TMP to delete`，用完主动清理。
- 交付物使用能够说明主题的文件名。
- 如建立 Git 仓库，提交说明主要使用中文。
- 浏览器自动化结束后关闭 browser/context/server，并检查无 Playwright 或 Chrome for Testing 残留进程。
