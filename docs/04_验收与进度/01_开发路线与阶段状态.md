# 项目测算分析工具：开发阶段与任务清单

更新时间：2026-08-02
状态：持续维护

> 本文件是阶段范围和完成状态的唯一执行基线。原型只用于理解布局，不代表功能已经进入开发范围。

## 一、当前架构结论

同一套 React + TypeScript 源码保留两种运行能力：

- 正式版：本地 TypeScript 服务托管前端，并把页面查询和写入实时同步到 `data/amoya_project_forecast.db`。
- 兼容单 HTML：内存 SQLite + 数据库文件手动导入导出，仅按需构建。

当前已形成的最小链路：

```text
真实项目与六类主数据（P0）
  → 损益、现金计划预测行配置（P1A）
  → 项目参数与行项目公式（P1B）
  → 税口径与收付款规则（P1C）
  → 项目聚合保存与期间覆盖（Schema v7）
  → 预测编译与计算批次
  → SQLite多维基础事实
  → 系统指标查询计算
  → 可编辑计算工作表
  → 页面、Excel 与打印报告
```

不建设跨项目分析。用户真实项目不会自动附加参考事实；第一次真实“保存并计算”后，当前项目工作版只使用新批次结果。

## 二、P0：SQLite 数据与报表验证版

### P0-A 工程、运行库与迁移

以下 PWA/OPFS 条目记录 P0 当时的已完成实现，当前正式运行方式已由“本地服务 + `.db` 文件”替代，数据契约和 Schema 迁移继续复用。

- [x] React + TypeScript + Vite 单包工程。
- [x] 官方 SQLite WASM 在 Dedicated Worker 中运行。
- [x] OPFS 固定数据库文件和运行状态检测。
- [x] `sys_schema_migration` 幂等 SQL 迁移。
- [x] 移除 Dexie/IndexedDB 新运行库依赖。
- [x] 保留旧 IndexedDB，不做跨 Origin 自动迁移。
- [x] PWA Manifest、Service Worker、离线预缓存和跨源隔离响应头。
- [x] 真正的单个 HTML 便携构建，使用内存 SQLite。
- [x] `.sqlite3` 完整导入、导出、文件头与 Schema 版本校验。

### P0-B 数据模型

- [x] 表前缀统一为 `dim_`、`fact_`、`sys_`，预留 `cfg_`、`snap_`。
- [x] 后台字段使用 `period`，前端显示“期间”。
- [x] `dim_project` 作为项目唯一主表，不维护同步副本。
- [x] 建立部门、业务模块、期间、场景、版本和指标维度。
- [x] 期间初始化 2020-01 至 2075-12，可覆盖现金尾期。
- [x] 每项目自动创建公共模块；基准场景和工作版调整为全局维度。
- [x] 事实唯一坐标包含项目、部门、模块、期间、场景、版本、指标。
- [x] 金额以十进制字符串保存。
- [x] 页面和业务服务通过仓储抽象访问数据库。

### P0-C 真实主数据与历史项目配置

- [x] 部门新增、修改、启用和停用。
- [x] 项目新增、修改、归档和恢复，不提供物理删除。
- [x] 业务模块在项目表单维护，在主数据页面只读查看。
- [x] 六份历史材料逐一复核；其中 5 份提炼为项目，1 份识别为 CDN/广告敏感性分析模型。
- [x] 移除两个项目的近似结构验证事实。
- [x] 五个历史项目均集中保存真实配置，并由计算服务生成事实。
- [x] 河北联通经营期校正为 17 个月并保留 3 个月现金回收期；百视通成本恢复四类明细。
- [x] 初始化幂等、事务化清理、用户数据隔离。
- [x] 用户真实项目默认没有事实数据。

### P0-D 指标与报表

- [x] 基础指标：收入、成本、现金流入、现金流出。
- [x] 系统计算指标：毛利、毛利率、净现金流、累计现金流。
- [x] 系统计算指标不回写事实表。
- [x] 最大垫资和现金转正期间作为周期级报告结果。
- [x] 收入为零、缺月、无需垫资和周期内未转正边界处理。
- [x] 项目“计算表格”开放为只读结果页。
- [x] 项目报表展示核心指标、分月损益、分月现金流和公式说明。
- [x] 业务模块筛选与项目数据隔离。

### P0-E 信息架构与正式 UI

- [x] 保持“外层项目中心—内层项目工作区”两层结构。
- [x] 主数据管理只保留项目、部门、业务模块、期间、场景、版本。
- [x] 指标管理独立入口和统一指标表。
- [x] 从主数据移除基础事实、计算预览和重复指标定义。
- [x] Lucide SVG 图标替换 Unicode 占位图标。
- [x] 正文、表格、表单和按钮以 12px 为主；标题和关键指标为 14px。
- [x] 32px 控件、36px 导航、约 40px 表格行。
- [x] 1280×720 浏览器下项目报表无关键遮挡。
- [x] 便携模式持续提示关闭前导出数据库。
- [x] P0 结束时预测配置保持禁用，未提前混入业务逻辑。

### P0-F 自动化与构建验证

- [x] 空库迁移、重复启动和表结构测试。
- [x] 项目、部门、全局维度和唯一约束测试。
- [x] 参考数据幂等、清理隔离和项目事实隔离测试。
- [x] Decimal 指标与边界测试。
- [x] SQLite 导出恢复测试。
- [x] 普通 PWA 构建。
- [x] 单 HTML 构建。
- [x] 浏览器验证 OPFS、项目计算表格、项目报表、六类主数据和便携模式。

P0 收口自动化结果：2 个测试文件、12 项用例通过；普通 PWA 与单 HTML 构建通过。
P0 收口提交：`31d4a28 校正P0全局维度与历史项目主数据`，已推送 `main`。

### P0-G 契约冻结

- [x] 冻结项目、部门和业务模块字段。
- [x] 确认期间、场景、版本均为平台级维度，项目通过事实坐标引用。
- [x] 冻结基础指标和系统计算指标定义。
- [x] 冻结 `fact_metric_value` 的事实坐标和报表查询口径。
- [x] 冻结 P1 输入到基础事实的分层接口：配置 → 行项目结果 → 指标事实。

## 三、P1：预测配置闭环

### P1A 损益与现金计划配置闭环

#### Schema v3/v4 与接口

- [x] `cfg_forecast_line` 保存行项目定义、分类、业务模块、预测方式、生效期间和说明。
- [x] `cfg_forecast_value` 保存逐月填写值。
- [x] `fact_forecast_line_value` 保存计算批次下的行项目级分月结果。
- [x] 行项目结果保存编码、名称和分类快照，配置改名或删除后仍可追溯旧批次。
- [x] `sys_calculation_run` 保存批次号、状态、配置摘要、问题和起止时间。
- [x] `fact_metric_value` 增加计算批次关联。
- [x] Schema v1、v2 可自动升级到 v3；恢复数据库后继续幂等迁移。
- [x] Schema v4 将配置行和行项目事实扩展到现金流入、现金流出。

#### 预测配置与编译

- [x] 收入、成本、收款、付款行自动映射四类基础指标。
- [x] 行项目稳定编码由系统生成。
- [x] 固定月金额按生效期间展开。
- [x] 逐月填写按项目期间展示，空白月按 0 计算并返回提示。
- [x] 金额允许负数并返回提醒。
- [x] 编译器为纯确定性逻辑，不调用 AI。
- [x] 保存草稿不覆盖上一批成功结果，并依据配置摘要标记结果过期。
- [x] 保存并计算创建批次、写入行项目结果、汇总基础事实，并在一个结果事务中生效。
- [x] 计算失败保留草稿和上一批成功结果，不写入半成品事实。
- [x] 新成功批次只替换当前项目“基准场景 + 工作版”的基础事实。

#### 页面闭环

- [x] 预测配置采用左侧主表、右侧无蒙层挤压表单。
- [x] 支持新增收入、成本、收款、付款项，支持编辑、确认删除、上移和下移。
- [x] 项目中心不再提供重复“编辑”入口；进入项目后在配置页直接维护项目信息。
- [x] 项目周期和业务模块修改增加预测行引用保护。
- [x] 支持保存草稿、保存并计算。
- [x] 固定月金额显示月金额、生效期间和周期合计。
- [x] 逐月填写显示已填/未填月份和合计，支持键盘连续录入。
- [x] 保存并计算成功后进入计算表格。
- [x] 计算表格显示批次号、最后计算时间、配置一致状态和返回配置入口。
- [x] 计算表格可按行项目追溯分月损益和现金流组成。
- [x] 项目报表继续只读取 `fact_metric_value`。
- [x] 有逐月现金计划时生成现金流事实；没有现金计划时不展示 0 元假结果。

#### 验证状态

- [x] 浏览器完成“固定月收入 + 逐月成本 → 保存并计算 → 行项目明细 → 分月损益”闭环验证。
- [x] 两种预测方式、空白按 0 和毛利/毛利率结果人工核对一致。
- [x] Schema、计算事务、草稿过期、失败回滚、备份恢复自动化测试全部通过。
- [x] 1280×720、1366×768 关键页面最终回归。
- [x] PWA、单 HTML 构建和便携数据库交换最终回归。
- [x] P1A 实现、文档与验证结果提交并推送 `main`。

P1A 历史项目回放回归结果：3 个测试文件、20 项用例通过；四个项目均由已保存配置生成报表并核对关键结果；PWA 与单 HTML 构建通过。

结果核对详见：[P1A 历史项目回放与结果核对报告](02_P1A_历史项目回放与结果核对报告.md)。

六份源文件的最新总核对及能力覆盖详见：[六份历史素材回放与能力覆盖核对报告](07_六份历史素材回放与能力覆盖核对报告.md)。

### P1B 参数与行项目公式

#### Schema v5 与参数模型

- [x] 新增 `cfg_parameter` 和 `cfg_parameter_value`。
- [x] 支持固定参数和逐月参数。
- [x] 支持金额、数量、比例和普通数值。
- [x] 参数编码按项目生成并保持稳定。
- [x] 比例在界面按百分数录入，在数据库按小数保存。
- [x] 被公式引用的参数和行项目增加删除保护。
- [x] Schema v1 至 v4 数据库可幂等升级到 v5。

#### 通用公式与计算追溯

- [x] 公式支持四则运算、括号、负数和百分数字面量。
- [x] 支持 `PARAM(...)` 引用项目参数和 `LINE(...)` 引用其他行项目。
- [x] 使用 Decimal.js 按期间计算，不执行 JavaScript、SQL或自定义函数。
- [x] 建立行项目依赖图和拓扑计算顺序。
- [x] 缺失引用、逐月参数缺值、除零、语法错误和循环引用阻止计算。
- [x] 参数与公式纳入 `config_hash`，配置变化后标记结果过期。
- [x] 计算批次保存不可变 `config_snapshot_json`。
- [x] 计算失败保留草稿和上一批成功结果。

#### 页面闭环

- [x] 预测配置增加“预测行项目”和“项目参数”二级页签。
- [x] 参数表和公式行均采用左表右侧无蒙层挤压式编辑。
- [x] 提供“单价 × 数量”和“收入 × 比例”常用模板。
- [x] 公式编辑显示中文解释、依赖来源、分月试算和定位错误。
- [x] 计算表格显示公式来源、上游参数和当前批次配置快照。
- [x] 项目报表继续只读取 `fact_metric_value`，不在页面重复执行公式。

#### 历史项目升级与回归

- [x] 河北有线的用户数、单价、支付比例、渠道分成比例和 CDN 单价已提炼为参数，收入成本改为公式行。
- [x] 重庆移动的注册用户改为逐月参数，收入和成本改为参数公式。
- [x] 河北联通的固定服务费用改为参数公式，服务器、CP分成和现金计划保留原配置。
- [x] 百视通月度资金计划保持原口径，未强行还原失效跨表公式。
- [x] 四个项目迁移前后收入、成本和现金计划核对基线一致。
- [x] PWA 与单 HTML 均完成参数公式计算、批次追溯和数据库交换回归。
- [x] 自动化测试、普通构建和单 HTML 构建全部通过。

P1B 收口自动化结果：4 个测试文件、27 项用例通过；Schema v5、公式解析、依赖计算、历史项目回放和数据库备份恢复均通过。

验收结果详见：[P1B 参数与公式验收报告](03_P1B_参数与公式验收报告.md)。

### P1C 税与现金流规则

#### Schema v6 与金额口径

- [x] 收入、成本行支持未税、含税和免税口径。
- [x] 税率数据库按小数保存，页面按百分数录入。
- [x] 公式引用其他行时读取标准化后的未税金额。
- [x] 既有行默认未税、税率 0，保持 P1B 损益基线。
- [x] 新增收付款规则、分期明细和现金计划追溯事实表。
- [x] Schema v1 至 v5 数据库可幂等升级到 v6。

#### 收付款编译

- [x] 支持不自动生成、当月 100%、延后 N 个月和自定义分期。
- [x] 分期支持 1～12 期、0～36 个月偏移并校验比例合计。
- [x] 最后一期吸收 6 位小数尾差。
- [x] 收入自动生成现金流入，成本自动生成现金流出。
- [x] 规则现金与直接现金分别追溯、统一汇总。
- [x] 配置哈希和批次快照包含税率、金额口径和收付款规则。
- [x] 失败回滚并保留上一批成功结果。

#### 页面与报表

- [x] 预测配置拆分为损益行项目、直接现金计划和项目参数。
- [x] 损益行右侧面板拆分“金额与公式 / 税与收付款”。
- [x] 二级页签固定在面板顶部，长表单滚动时仍可切换。
- [x] 展示未税额、税额、含税结算额和按月结算预览。
- [x] 计算表格增加规则现金追溯。
- [x] 自动及直接现金允许延伸到经营期后 36 个月。
- [x] 项目报表自动延长至最后现金期间，并标记经营期和回收期。
- [x] 最大垫资和现金转正期间使用延长后的现金事实计算。

#### 验证状态

- [x] 含税收入 6% 延后 3 个月收款浏览器闭环。
- [x] 未税成本 6% 分两期付款浏览器闭环。
- [x] 经营期后现金尾期和项目报表回收期人工核对。
- [x] Schema、税换算、收付款规则、事务、备份恢复自动化测试。
- [x] PWA 和单 HTML 构建通过。

P1C 收口自动化结果：5 个测试文件、36 项用例通过；Schema v6、税口径、收付款规则、现金尾期和数据库备份恢复均通过。

验收结果详见：[P1C 税口径与现金流验收报告](04_P1C_税与现金流验收报告.md)。

### 运行方式收敛：本地服务与可见 DB 文件

- [x] 新增 TypeScript 本地服务，绑定 `127.0.0.1:4173` 并托管 `output/web/`。
- [x] 页面统一通过语义化业务接口查询、保存、计算和备份恢复；SQL 只保留在服务端。
- [x] 写操作完成后立即同步到 `data/amoya_project_forecast.db`，服务重启后恢复数据。
- [x] 服务端串行数据库操作，并使用临时文件替换避免半写入主文件。
- [x] 增加随机启动令牌、同源请求校验和最大请求体限制。
- [x] 增加 `pnpm start:local`、Windows `.cmd` 和 macOS `.command` 启动入口。
- [x] 顶栏和数据管理区域显示本地服务、数据库文件名及自动保存状态。
- [x] 默认 `output/web/` 收敛为一个 HTML、一个 JavaScript 和一个 CSS 文件。
- [x] macOS 完成保存落盘、服务重启恢复、浏览器控制台和自动化回归。
- [ ] 在目标 Windows 电脑完成 Node.js、启动脚本、保存落盘和重启恢复真机验收。

当前服务使用现有官方 SQLite WASM 运行库在 Node.js 内管理数据库，并在每次写操作后导出标准 SQLite 文件，因此不依赖原生扩展，也不要求 Node.js 24。后续仅在 `node:sqlite` 进入更稳定状态且完成 Windows 验证后再评估更换驱动；仓储和业务计算口径不随驱动变化。

验收结果详见：[本地服务与 DB 文件存储改造验收报告](05_本地服务与DB文件存储改造验收报告.md)。

### 项目核心重构（Schema v7）

- [x] 源码拆分为 `app/server/shared`，正式页面不再发送 SQL。
- [x] 建立项目、工作区、计算、部门、指标、报告和数据库备份恢复的语义化 API。
- [x] 删除正式服务的 `/api/database/query`、`execute` 和 `batch`。
- [x] 恢复项目、部门、业务模块、期间、场景和版本六类主数据工作台；部门继续通过语义接口维护。
- [x] 项目聚合草稿一次包含项目信息、模块、参数、预测行、税与现金规则和期间覆盖。
- [x] `draft_revision` 乐观锁冲突返回 409。
- [x] 顶部统一“保存”和“计算”，取消项目信息与预测配置分散保存。
- [x] 新建项目使用独立 URL 页面，项目配置、计算工作表和报告均支持刷新恢复。
- [x] `cfg_forecast_override` 保存叶子行期间覆盖，不修改原预测定义。
- [x] 覆盖值进入下游公式、税、现金计划和派生指标；清除覆盖恢复原计算。
- [x] `FinancialGrid` 支持框选、复制粘贴、清空、填充、撤销重做和只读保护。
- [x] 计算批次保存项目快照、完整配置快照和草稿修订号。

### P1D 统一交付

- [x] 冻结统一 `ProjectReportDto`，页面报告只读取成功批次。
- [x] 完成损益、现金和行项目构成图表，ECharts 本地打包。
- [x] 完成确定性测算摘要、风险提示、关键参数和人工覆盖说明。
- [x] 完成七表统一 Excel、覆盖值着色和原值批注。
- [x] 完成 A4 横向打印/PDF 样式。
- [x] 历史成功批次切换和报告过期状态。
- [ ] 目标 Windows Excel 真机打开、打印和批注验收。
- [ ] 第一轮真实财务用户试用。

项目核心与正式输出验收结果详见：[项目核心重构与正式输出验收报告](06_项目核心与正式输出验收报告.md)。

## 四、P2：多场景与版本快照

- [ ] P2A：ROI、可见批量填充、CSV/TXT 导入和历史批次恢复。
- [ ] P2B：Schema v8、项目场景关系、场景配置隔离及场景复制。
- [ ] P2C：场景比较、融合报告和多工作表 Excel。
- [ ] 多场景工作版稳定后，再规划只读版本快照和版本比较。

完整范围与顺序详见：[V3.1 增量与多场景实施计划](../05_后续计划/02_V3.1增量与多场景实施计划.md)。

## 五、当前明确不做

- 跨项目报表和自由透视。
- 多人协作、权限、审核和审批。
- 工具内置 AI。
- 任意脚本公式。
- 日期级资金计划。
- 客户级 Excel 模板定制。
- Electron、Tauri、DMG、EXE 或 SQLite 之外的第二套业务存储。
- 完整增值税核算、多场景、版本快照、Excel 输入或客户定制 Excel 模板。
