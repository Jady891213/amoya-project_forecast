# Schema v14：事实调整与最新结果验收报告

验收日期：2026-08-04

软件版本：v0.5.5

数据结构：Schema v14

## 1. 结构结果

- 已重建当前开发库并初始化 5 个参考项目、5 个“方案 1”。
- `dim_plan` 已删除 `is_default`。
- 已删除 `sys_calculation_run` 和 `cfg_forecast_override`。
- 已新增 `sys_plan_calculation_state` 和 `fact_metric_adjustment`。
- 原始行项目事实和现金计划不再包含计算 Run 外键。

重建前的 v13 开发库保存在项目根目录：`TMP to delete Schema v13 开发库备份 2026-08-04.db`，仅用于短期回查，不提交仓库。

## 2. 行为结果

- 项目配置保存不读写人工调整。
- 计算成功替换当前 Plan 最新原始结果，失败保留上一版成功结果。
- 人工调整保存后直接更新最终基础事实、派生指标、项目报告和项目报表，无需再次计算。
- 人工调整不改变预测项之间的公式结果，也不重新生成自动现金计划。
- 重新计算后人工调整绝对值继续保留；清除调整恢复当前原始值。
- 未指定 Plan 时稳定进入排序最前的有效方案。
- 项目工作区已提供六步真实页面截图操作指引，覆盖方案、配置、计算、底稿调整、报告查看和下载。

## 3. 自动化与构建

- `pnpm test`：14 个测试文件、57 个用例全部通过。
- `pnpm build`：正式 Web 构建通过。
- `pnpm build:legacy-singlefile`：兼容单 HTML 构建通过。
- 本地服务：`http://127.0.0.1:4173/projects` 返回 200。
- Playwright 截图流程结束后已关闭浏览器，并确认无自动化浏览器残留进程。

## 4. 已知边界

- 当前不保存历史 Run 结果，也不提供历史计算切换。
- 人工调整是最终事实层调整，不会反向改变项目配置或预测公式。
- 开发阶段不提供 v13 到 v14 自动迁移；旧库应使用对应旧版本回查或重新初始化。
